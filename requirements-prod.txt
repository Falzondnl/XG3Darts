# ============================================================
# XG3 Darts Microservice — PRODUCTION runtime dependencies only
# Heavy ML training deps (torch, pymc, catboost, ray, numba, etc.)
# are excluded — they are only needed for offline training scripts.
# ============================================================

# ---- Web Framework ----
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# ---- Database ----
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
greenlet>=3.0.0
psycopg2-binary>=2.9.0

# ---- Cache ----
redis[hiredis]>=5.0.0

# ---- Observability ----
prometheus-client>=0.19.0

# ---- Logging ----
structlog>=23.0.0
python-json-logger>=2.0.0

# ---- HTTP ----
aiohttp>=3.9.0
httpx>=0.26.0

# ---- Web Scraping (feeds: Flashscore + Darts24 live data) ----
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ---- Data Science ----
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.11.0

# ---- Machine Learning (inference only) ----
scikit-learn>=1.4.0
lightgbm>=4.2.0
joblib>=1.3.0

# ---- Environment ----
python-dotenv>=1.0.0
